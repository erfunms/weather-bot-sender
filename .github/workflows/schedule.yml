name: Weather Auto Sender ☀️

on:
  schedule:
    - cron: '0 6 * * *'     # هر روز ساعت 6 صبح UTC
    - cron: '0 18 * * *'    # هر روز ساعت 6 عصر UTC
  workflow_dispatch: {}

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          pip install requests jdatetime
          echo "✅ Installed packages:"
          pip list

      - name: 📅 Run weather sender script
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
          CHAT_IDS: ${{ secrets.CHAT_IDS }}
          REGION_NAME: ${{ secrets.REGION_NAME }}
          IMAGE_URL: ${{ secrets.IMAGE_URL }}
        run: |
          echo "🚀 Starting Weather Bot at $(date -u)"
          python -c "import jdatetime; print('✅ jdatetime imported successfully!')"  # تست قبل از اجرای اصلی
          python send_weather.py || {
            echo "❌ Error: Script failed!"
            exit 1
          }
          echo "✅ Weather Bot finished successfully at $(date -u)"
