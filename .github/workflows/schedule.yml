name: Weather Auto Sender ☀️

on:
  schedule:
    - cron: '0 6 * * *'      # هر روز ساعت 6 صبح UTC
    - cron: '0 18 * * *'     # هر روز ساعت 6 عصر UTC
  workflow_dispatch: {}

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' # یا هر نسخه 3.x دیگری که ترجیح می‌دهید

      - name: 📦 Install dependencies (Force)
        # استفاده از دستور کامل پایتون برای دور زدن مشکلات PATH و Cache
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install requests jdatetime
          echo "✅ jdatetime installed successfully."
          
      - name: 📅 Run weather sender script
        env:
          # --- متغیرهای محیطی که از Secrets مخزن می‌آیند ---
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
          CHAT_IDS: ${{ secrets.CHAT_IDS }}
          REGION_NAME: ${{ secrets.REGION_NAME }} # اگر به عنوان Secret تعریف شده
          IMAGE_URL: ${{ secrets.IMAGE_URL }}     # اگر به عنوان Secret تعریف شده
          # اگر از LAT/LON استفاده می‌کنید، باید اینجا تعریف شوند:
          # LAT: ${{ secrets.LAT }}
          # LON: ${{ secrets.LON }}
          
        run: |
          echo "🚀 Starting Weather Bot at $(date -u)"
          python send_weather.py || {
            echo "❌ Error: Script failed!"
            exit 1
          }
          echo "✅ Weather Bot finished successfully at $(date -u)"

