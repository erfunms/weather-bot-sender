name: Weather Auto Sender ☀️

on:
  schedule:
    - cron: '30 3 * * *'      # ⬅️ 7:00 صبح ایران = 3:30 بامداد UTC
    - cron: '30 18 * * *'     # ⬅️ 10:00 شب ایران = 18:30 عصر UTC
  workflow_dispatch: {}

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies (FINAL FIX)
        run: |
          # استفاده از python3 -m pip برای حل قطعی مشکل ModuleNotFoundError
          python3 -m pip install --upgrade pip
          python3 -m pip install requests jdatetime # ⬅️ حل کننده مشکل jdatetime
          echo "✅ jdatetime installed successfully."
          
      - name: 📅 Run weather sender script
        env:
          # --- Secrets اصلی ---
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          OPENWEATHER_KEY: ${{ secrets.OPENWEATHER_KEY }}
          CHAT_IDS: ${{ secrets.CHAT_IDS }}
          REGION_NAME: ${{ secrets.REGION_NAME }}
          IMAGE_URL: ${{ secrets.IMAGE_URL }}
          
          # ⬅️ افزودن Secret جدید برای AQICN (جهت دقت بالاتر AQI)
          AQICN_TOKEN: ${{ secrets.AQICN_TOKEN }}
          
        run: |
          echo "🚀 Starting Weather Bot at $(date -u)"
          python3 send_weather.py || { # ⬅️ استفاده از python3 برای اجرای اسکریپت
            echo "❌ Error: Script failed!"
            exit 1
          }
          echo "✅ Weather Bot finished successfully at $(date -u)"
